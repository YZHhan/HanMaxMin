<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.han.hanmaxmin">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".HanApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/GC_Theme">
        <activity
            android:name=".hantext.mvptest.activithy.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.listview.Main2Activity">
        </activity>
        <activity android:name=".activity.Main3Activity">
        </activity>
        <activity android:name=".activity.listview.recycler.RecyclerActivity">
        </activity>
        <activity android:name=".activity.viewpager.ViewPagerActivity">
        </activity>

        <activity android:name=".hantext.mvptest.MineActivity">
        </activity>

    </application>

</manifest>